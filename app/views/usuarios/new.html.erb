<h1>Novo Usuário</h1>

<% form_for(@usuario) do |f| %>
  <%= render :partial => 'formulario', :locals => { :f => f } %>
  <p>
    <%= f.submit 'Criar' %>
  </p>
<% end %>

<%= link_to 'Todos', usuarios_path %>
